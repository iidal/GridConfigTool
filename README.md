# GridConfigTool

first version, basic tool with
 - resizable grid
 - only supports integers in the grid values
 - configurable amount of int values than can be used in the grid
 - save config as JSON or scriptable object
  